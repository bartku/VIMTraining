" Use Vim settings, not Vi settings (this should
" be the first line in your config)
set nocompatible

" Sane setting for backspace key
set backspace=2

" Save 1,000 items in history
set history=1000

" Show the line and column number of the cursor position
set ruler

" Show a status bar
set laststatus=2

" Display the incomplete commands in the bottom right-hand side of your screen.  
set showcmd

" Display completion matches on your status line
set wildmenu

" Show a few lines of context around the cursor
set scrolloff=5

" Highlight search matches
set hlsearch

" Enable incremental searching
set incsearch

" Ignore case when searching
set ignorecase

" Override the 'ignorecase' option if the search pattern contains upper case characters.
set smartcase

" Turn on line numbering
set number

" Turn on relative line numbers
set relativenumber

" Don't line wrap mid-word.
set lbr

" Copy the indentation from the current line.
set autoindent

" Enable smart autoindenting.
set smartindent

" Use spaces instead of tabs
set expandtab

" Enable smart tabs
set smarttab

" Make a tab equal to 4 spaces
set shiftwidth=4
set tabstop=4

" Show mode Vim is in
set showmode

" No need for a swap/backup file in the 21st century
set nobackup
set noswapfile

" Keep settings and history between sessions
set viminfo=!,'100,<50,s10,h

" Specifiy a color scheme.
colorscheme desert

" Show colors
syntax on

" Map Y to act like D and C, i.e. yank until EOL, rather than act like yy
map Y y$

map <up> <NOP>
map <down> <NOP>
map <left> <NOP>
map <right> <NOP>
imap <up> <NOP>
imap <down> <NOP>
imap <left> <NOP>
imap <right> <NOP>

" change the mapleader from \ to ,
" NOTE: This has to be set before <leader> is used.
" NOTE: A good option for <leader> is also SPACE
let mapleader=','

" Quickly save your file.
map <leader>w :w!<cr>

" For more options see ":help option-list" and ":options".
